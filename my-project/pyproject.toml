[project]
name = "my-project"
version = "0.1.0"
description = ""
authors = [
    {name = "Muriithi Gichuki",email = "107644777+GichukiEdwin@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask (>=3.1.2,<4.0.0)"
]

[tool.poetry]
packages = [{include = "my_project", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
